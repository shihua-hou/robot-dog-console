红激光必须对齐黑色障碍。匹配失败时请设初始位姿，系统会在附近精修。

在板子本机终端执行（不要用 Cursor Agent 终端）：

  bash /home/linaro/robot_ws/scripts/fix_nav.sh map_live.yaml

然后刷新网页：
  1. 若仍显示「待定」→ 点「设初始位姿」，按住拖朝向松手
  2. 等提示「已对齐(贴墙)」且红点贴黑墙后再「设目标」
